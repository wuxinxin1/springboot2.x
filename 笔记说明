1.关于resources下面的文件夹
  a.static存放静态文件，如js,imag,css
  b.templates存放模板引擎如html,jsp，thymeleaf等
  c.config存放配置文件
  d.静态文件的加载顺序:META-INF/resources->resources->static->public
  e.如果要使用templates下面的模板引擎功能，需要添加依赖
      <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-thymeleaf</artifactId>
       </dependency>
  f:想要修改静态资源加载目录，需要修改配置spring.resources.static-locations=["classpath:/testpublic/"]